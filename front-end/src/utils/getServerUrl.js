// require('dotenv').config();

// const url = process.env.URL_SERVER;
// console.log(url);

const getServerUrl = () => (
  // const url = process.env.URL_SERVER;
  'http://localhost:3001'
);

module.exports = getServerUrl;
